[package]
name = "caesar"
version = "0.1.0"
authors = ["lee"]
edition = "2018"

[workspace]
members = [
    "crates/vdom",
    "crates/browser",
    "examples/isomorphic/client",
]

#[profile.release]
# Tell `rustc` to optimize for small code size.
#opt-level = "s"
